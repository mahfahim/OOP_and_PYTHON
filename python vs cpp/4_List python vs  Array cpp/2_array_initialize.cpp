#include <iostream>
using namespace std;

int main() {
    // Declare an array of size 5, initialized with the first element as 100
    int ar[5] = {100};

    // Print the array elements
    for (int i = 0; i < 5; i++) {
        cout << ar[i] << endl;
    }

    return 0;
}
