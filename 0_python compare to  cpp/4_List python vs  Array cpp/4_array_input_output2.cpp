#include <iostream>
using namespace std;

int main() {
    int ar[5];

    // Input values into the array
    for (int i = 0; i < 5; i++) {
        cin >> ar[i];
    }

    // Print the values of the array
    for (int i = 0; i < 5; i++) {
        cout << ar[i] << endl;
    }

    return 0;
}
